# Hardening for release

Started 2026-09-22, before the first outside company uses Cira. Five
read-only reviews traced the code the way a real business would use it
(access, people joining and leaving, deploying, capabilities and agents,
money and alerts). Every finding they reported is below. Each finding is
reproduced before it is fixed. A fix lands with a test that failed before it.

This file is the record. When a finding is fixed, its line says how and where
the test lives. When one is deliberately left alone, it says why.

**Status:**

- **open** - not yet done
- **fixed** - done, with a test
- **won't fix** - left alone on purpose, with the reason
- **partly** - the part Cira controls is done, and the rest is named
- **later** - real, but it belongs to a later roadmap item, which is named

**Severity** is how badly a company would be hurt the first time it happened:

- **critical** - lost data, secrets exposed, or someone acting where they have no right
- **high** - an outage, or money lost
- **medium** - a broken workflow
- **low** - rough edges

## Access and security

| ID     | Sev      | Finding                                                                                                                                                                                                        | Status |
| ------ | -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| SEC-1  | critical | Any member can redeploy any app in their space, including apps they cannot open, by its id (which `.cira/project.json` commits). Only setting env was checked.                                                 | fixed  |
| SEC-2  | high     | `/api/cli/capabilities` and `/verify` only check membership, so any member can rewrite or probe the capabilities of an app they cannot see.                                                                    | fixed  |
| SEC-3  | critical | The CLI uploads files `.gitignore` excludes (`.streamlit/secrets.toml`, `config/master.key`, `credentials.json`, `*.pem`, `.npmrc`), bakes them into images and sends text files to the model during analysis. | fixed  |
| SEC-4  | high     | CLI and assistant tokens never expire. An assistant token can deploy and remove apps.                                                                                                                          | fixed  |
| SEC-5  | high     | Joining by email domain has no approval and no off switch. It is on for every new space, and the list of personal-mail domains is short.                                                                       | fixed  |
| SEC-6  | high     | Re-linking a new sign-in by email lets whoever next holds an address inherit a former person's account, across companies.                                                                                      | fixed  |
| SEC-7  | medium   | The old-address redirect for a renamed app reveals the new name to people with no access.                                                                                                                      | fixed  |
| SEC-8  | low      | `/api/cli/source` signs 100 MB uploads for any signed-in person, with no space and no limit.                                                                                                                   | fixed  |
| SEC-9  | low      | `/api/cli/deploy/status` checks space membership, not access to the app.                                                                                                                                       | fixed  |
| SEC-10 | low      | `spaceMemberCount` is an exported server action with no auth check.                                                                                                                                            | fixed  |
| SEC-11 | low      | A browser session is bound to an app's address, not the app, so a new restricted app at a reused address opens for up to 15 minutes to someone who could open the old one.                                     | fixed  |

## People

| ID        | Sev      | Finding                                                                                                                                                     | Status |
| --------- | -------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| PEOPLE-1  | critical | Nobody can be removed from a space, change role, or leave. Nothing can move an app to a new owner. There is only ever one owner.                            | fixed  |
| PEOPLE-2  | medium   | Pending invites cannot be listed or revoked in the product.                                                                                                 | fixed  |
| PEOPLE-3  | medium   | Teams cannot be created or edited in the product, so granting access to a team is impossible for a real company.                                            | fixed  |
| PEOPLE-4  | high     | A name can produce a slug that ends in `-` or runs past 48 characters, making a space or app that cannot be opened or deleted.                              | fixed  |
| PEOPLE-5  | high     | A space can take a slug that one of Cira's own routes already uses (`docs`, `pricing`, `legal`, `monitoring` and others), and then it can never be reached. | fixed  |
| PEOPLE-6  | low      | Names written only in non-Latin scripts are refused.                                                                                                        | fixed  |
| PEOPLE-7  | low      | Someone in several spaces lands in an arbitrary one.                                                                                                        | fixed  |
| PEOPLE-8  | medium   | Over MCP, a person in two companies sees capabilities named the same in both, with nothing saying which company each belongs to.                            | fixed  |
| PEOPLE-9  | low      | Double-clicking accept on an invite, or creating two spaces with the same name at once, shows a raw error.                                                  | fixed  |
| PEOPLE-10 | medium   | Someone joining from an invite was never asked their name, so they were their email address everywhere - twice on the members page.                         | fixed  |
| PEOPLE-11 | high     | A company could not use its own identity provider to sign in, or to take someone out of Cira when they left.                                                | fixed  |

## Deploying and running

| ID        | Sev      | Finding                                                                                                                                                                                                      | Status |
| --------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------ |
| DEPLOY-1  | critical | A redeploy with no local `.env` (a teammate's clone, or CI) wipes every production variable before the build starts.                                                                                         | fixed  |
| DEPLOY-2  | high     | Env and process changes reach the running app before the build finishes. If the build fails they stay applied, and the email says nothing changed.                                                           | fixed  |
| DEPLOY-3  | high     | Overlapping deploys of one app can roll production back to the older build.                                                                                                                                  | fixed  |
| DEPLOY-4  | critical | Removing an app, or its whole space, leaves its worker pools, jobs and scheduler entries running on Google, still billing and still holding the app's secrets.                                               | fixed  |
| DEPLOY-5  | high     | Renaming an app orphans its Cloud Run service, and the next deploy creates a second one.                                                                                                                     | fixed  |
| DEPLOY-6  | high     | No variable reaches the build, so `NEXT_PUBLIC_*` and `VITE_*` values are missing from browser bundles.                                                                                                      | fixed  |
| DEPLOY-7  | high     | The upload drops `build/`, `dist/`, `out/` and `target/`, which breaks Dockerfiles that copy from them.                                                                                                      | fixed  |
| DEPLOY-8  | high     | A failed first deploy leaves a failed app behind, so every retry makes another ("My App 2" and so on).                                                                                                       | fixed  |
| DEPLOY-9  | medium   | Only the first env file found is read, instead of the standard layering of `.env`, `.env.production`, `.env.local` and `.env.production.local`.                                                              | fixed  |
| DEPLOY-10 | medium   | The CLI gives up after 10 minutes while builds may run for 20. A long build can be marked failed while its code is serving.                                                                                  | fixed  |
| DEPLOY-11 | medium   | When a container fails to start, Google's reason is dropped, so the person is sent to build logs that show success.                                                                                          | fixed  |
| DEPLOY-12 | medium   | A process that keeps its name but changes kind (worker to scheduled, or back) leaves the old resource running.                                                                                               | fixed  |
| DEPLOY-13 | medium   | A failed process write is swallowed. Rows are already replaced, so a worker taken out of the Procfile can keep running unseen.                                                                               | fixed  |
| DEPLOY-14 | medium   | Listing workers and jobs reads one page of 500. Past that, a running worker can be missed, and even switched off by a deploy.                                                                                | fixed  |
| DEPLOY-15 | medium   | Web apps are fixed at 512 MiB of memory, and `web` sizes in fly.toml and app.json are ignored.                                                                                                               | fixed  |
| DEPLOY-16 | medium   | The deploy route has a 60 second limit. Many processes can exceed it and leave an app stuck in "deploying".                                                                                                  | fixed  |
| DEPLOY-17 | medium   | Removing `web:` from a Procfile leaves the old web service serving.                                                                                                                                          | fixed  |
| DEPLOY-18 | medium   | Monorepos in the default Turborepo layout are refused, or build without their workspace packages.                                                                                                            | fixed  |
| DEPLOY-19 | medium   | Static sites and SPAs with no server are deployed anyway, and then fail to build or start.                                                                                                                   | fixed  |
| DEPLOY-20 | low      | A Procfile `release:` line (migrations) is dropped without telling anyone.                                                                                                                                   | fixed  |
| DEPLOY-21 | low      | The dotenv parser mangles multiline values and inline comments. `--env-file=path` is ignored.                                                                                                                | fixed  |
| DEPLOY-22 | low      | `cira remove --yes` behaves differently from `cira deploy --yes`.                                                                                                                                            | fixed  |
| DEPLOY-23 | low      | Status polling swallows 401 and 404 for the full wait, and the upload has no timeout.                                                                                                                        | fixed  |
| DEPLOY-24 | low      | Two people switching on workers at the same moment can go past the plan's limit.                                                                                                                             | fixed  |
| DEPLOY-25 | medium   | A failed build printed forty lines of Cloud Build and Docker progress - layer hashes, timers, "Step #0:" on every line - with the error somewhere in the middle.                                             | fixed  |
| DEPLOY-26 | high     | A failed deploy marked a running app Failed and took its Open button away, while the build before went on serving; the page and app_status also said "live" through an outage the watcher had emailed about. | fixed  |
| DEPLOY-27 | low      | Every deploy ended "Only you can see it" and "its workers and scheduled runs are off", whatever was true: a redeploy of an app shared with everyone, with its runs on, said both.                            | fixed  |
| DEPLOY-28 | high     | An app that keeps anything needed a database from somewhere Cira could not see, set up by hand before the first deploy.                                                                                      | fixed  |
| DEPLOY-29 | high     | The app proxy asked Cira for the newest deploy, so from the moment a redeploy failed every app was "not reachable" through it while the build before went on serving.                                        | fixed  |
| DEPLOY-30 | high     | Incident, 2026-09-22, about 15 minutes: the app proxy was shipped with a proxy secret pulled from Vercel, which does not hand back sensitive values, and every app looped between Cira and the proxy.        | fixed  |
| DEPLOY-31 | medium   | An app that needs Redis had to bring one from somewhere Cira could not see.                                                                                                                                  | fixed  |

## Capabilities and agents

| ID     | Sev    | Finding                                                                                                                                                                           | Status |
| ------ | ------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| CAP-1  | high   | Read or write is whatever the model says. A `DELETE` it grades as a read switches itself on and runs without approval.                                                            | fixed  |
| CAP-2  | high   | MCP writes have no approval on Cira's side. The promise that changes wait for a person holds only in Ask Cira and the console.                                                    | fixed  |
| CAP-3  | high   | A read with a path parameter (`GET /customers/{id}`) is deleted when verification's made-up id gets a "no such record" 404.                                                       | fixed  |
| CAP-4  | high   | A write that succeeds but answers with a redirect, a non-JSON body, or late is reported as unreachable. That invites a retry and a duplicate.                                     | fixed  |
| CAP-5  | high   | A capability that changes method, path or risk on redeploy stays switched on. A read can become an unreviewed write.                                                              | fixed  |
| CAP-6  | medium | Ask Cira's daily limit can be bypassed with decision requests, and history sent from the browser is not capped.                                                                   | fixed  |
| CAP-7  | medium | A 302 to a login page, or an OPTIONS with no `Allow`, counts as callable in verification.                                                                                         | fixed  |
| CAP-8  | medium | Once headers arrive, the response body is read with no deadline, so a trickling endpoint hangs until the function is killed and no record is written.                             | fixed  |
| CAP-9  | medium | The 60-runs-a-minute limit is counted after calls finish, so parallel calls all pass.                                                                                             | fixed  |
| CAP-10 | medium | During a build, or after a failed deploy, every capability goes dark while the old revision is still serving.                                                                     | fixed  |
| CAP-11 | medium | A write body with non-ASCII characters sends a wrong `content-length` and fails.                                                                                                  | fixed  |
| CAP-12 | medium | Ask Cira reads capabilities from every space a person is in, not only the one it was opened in.                                                                                   | fixed  |
| CAP-13 | medium | Discovery on large apps overflows the output limit and yields nothing. The source budget is filled alphabetically, so route files can be left out.                                | fixed  |
| CAP-14 | medium | GET arrays are sent as JSON strings. Non-GET inputs all go in the body, even when the app reads them from the query.                                                              | fixed  |
| CAP-15 | low    | A path value of `.` collapses its segment.                                                                                                                                        | fixed  |
| CAP-16 | low    | Verifying as one person records a refusal for everyone.                                                                                                                           | fixed  |
| CAP-17 | low    | A capability whose route is gone is never demoted.                                                                                                                                | fixed  |
| CAP-18 | low    | MCP results are pretty-printed without a size cap, and the record does not say whether a person approved a write.                                                                 | fixed  |
| CAP-19 | high   | The same code, analysed twice, came back with different names (listOrders, then getOrders), and a renamed capability was a new one: its approval reset and agents lost it.        | fixed  |
| CAP-20 | medium | A capability that worked was never checked again, so a route that started refusing Cira in a new build went unnoticed until an agent called it.                                   | fixed  |
| CAP-21 | high   | A write on a server that never answers OPTIONS - a plain Node `http` server - was deleted as a route the app does not serve.                                                      | fixed  |
| CAP-22 | high   | Checking a capability could perform it: a write served on GET - `GET /api/visits`, which increments - was called by the check when the app would not say which methods it allows. | fixed  |

## Money

| ID      | Sev      | Finding                                                                                                                                    | Status |
| ------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------ | ------ |
| BILL-1  | critical | The trial never ends. Its end date is only displayed.                                                                                      | fixed  |
| BILL-2  | critical | Workers and warm apps switched on after checkout are never added to the subscription. Checkout breaks when the always-on price is missing. | fixed  |
| BILL-3  | critical | After a cancel, workers and warm apps keep running for free.                                                                               | fixed  |
| BILL-4  | high     | After an abandoned or cancelled checkout a space can never subscribe again: it only offers the portal.                                     | fixed  |
| BILL-5  | high     | Webhooks are applied in whatever order they arrive, for whichever subscription they name.                                                  | fixed  |
| BILL-6  | high     | Two admins, or two tabs, can create two customers and two subscriptions.                                                                   | fixed  |
| BILL-7  | high     | Deleting a space while a checkout is open leaves a subscription charging for a space that no longer exists.                                | fixed  |
| BILL-8  | high     | The five-minute health probe keeps apps with sidecars running all the time (they are billed per instance), and nobody pays for it.         | fixed  |
| BILL-9  | medium   | A subscription set to cancel at the end of its period, or one that has expired, still blocks deleting the space.                           | fixed  |
| BILL-10 | medium   | Cira never emails anyone about a failed payment or a cancellation, and Stripe's own emails go to whichever admin first clicked subscribe.  | fixed  |
| BILL-11 | medium   | Apps deleted this month disappear from this month's usage.                                                                                 | fixed  |
| BILL-12 | medium   | One person can open any number of trial spaces.                                                                                            | fixed  |
| BILL-13 | low      | Only one `v1` signature is checked, so rotating the webhook secret can fail.                                                               | fixed  |
| BILL-14 | low      | Coming back from checkout before the webhook arrives still shows "Trial".                                                                  | fixed  |
| BILL-15 | low      | Keep-warm can end up on at Google but off in Cira if the database write fails.                                                             | fixed  |

## Alerts

| ID      | Sev    | Finding                                                                                                                                                                                          | Status |
| ------- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------ |
| ALERT-1 | high   | An alert that fails to send is lost forever: it is claimed first and never retried. Resend's rate limit makes that likely in an outage.                                                          | fixed  |
| ALERT-2 | medium | Nothing limits how many emails a flapping app, a crashlooping worker, or a failing five-minute job sends.                                                                                        | fixed  |
| ALERT-3 | medium | A worker that crashes without running out of memory is never reported, but is still billed.                                                                                                      | fixed  |
| ALERT-6 | low    | A refused capability, in its email and on the app page, said no setting in Cira could help - untrue since apps can be told who is calling.                                                       | fixed  |
| ALERT-4 | medium | The billing sync runs last in the watcher, so a slow run can starve it.                                                                                                                          | fixed  |
| ALERT-5 | high   | Notices about a space would have gone to the demo company's invented admins, at a domain Cira does not own, and a stranger's inbox or a bounce against Cira's sending name is what that reaches. | fixed  |
| ALERT-7 | low    | Checking every alert for real on production: the failed-run email read "The scheduled run nightly of Notify E2e".                                                                                | fixed  |

## Found by hand

Found while using the product end to end, as a new company would.

| ID     | Sev      | Finding                                                                                                                                                                           | Status |
| ------ | -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| UX-1   | medium   | Sign-up is a bare Clerk card with no Cira mark, no mention of the trial, and no way back. It also asks for a password, although sign-in is described as email codes.              | fixed  |
| UX-2   | low      | Every page is titled "Cira", including sign-up and sign-in.                                                                                                                       | fixed  |
| E2E-1  | critical | Every deploy in production stopped as "superseded" before going out: the check for a newer deploy compared a millisecond copy of the row with its microsecond self.               | fixed  |
| E2E-2  | high     | `cira deploy --help` deployed the folder, and any unknown or misspelled flag was ignored rather than refused.                                                                     | fixed  |
| E2E-3  | low      | A workspace package deployed from its own folder was named after the repository, not the package.                                                                                 | fixed  |
| E2E-4  | medium   | A build that failed from the CLI said only that its logs were on the app's page; the terminal that started it never showed the error.                                             | fixed  |
| E2E-5  | medium   | When capability analysis failed, the CLI printed the model provider's raw error: status, JSON body and request id, about Cira's own account.                                      | fixed  |
| ENG-1  | medium   | The packages' tests were never typechecked, and ten of them had drifted from the types they test.                                                                                 | fixed  |
| E2E-6  | medium   | A deploy whose migration failed showed the build log, which had succeeded; the release's own output was nowhere a person could read it, and the CLI said "building" while it ran. | fixed  |
| E2E-7  | medium   | Pricing and the terms said a subscription that ends goes back to the trial allowance; since plans were enforced, a space past its trial stops its workers and cannot deploy.      | fixed  |
| E2E-8  | high     | The app page quoted what keeping an app warm or running a worker costs Cira ($49, about $50) as its price, while pricing bills $75 each.                                          | fixed  |
| E2E-9  | medium   | The logs page, and any app with scheduled runs, rendered times in UTC on the server and local time in the browser: a React hydration error, and times that jump.                  | fixed  |
| E2E-10 | medium   | With Clerk's heading hidden, the sign-in code step no longer said a code had been sent, or to which address.                                                                      | fixed  |
| E2E-11 | low      | The Homepage setting's placeholder was a real site from another project, the Billing page was titled Usage, and an approval read "Approve Regenerate report?".                    | fixed  |

## Fixes

What changed for each finding, and where its test is.

- **SEC-1** (fixed). Redeploying an existing app takes the right to manage it (`userManages` in `lib/app-rights.ts`), checked before anything is written or sent to Google. Manage is now also a level on a grant (`app_access.level`, migration 0031), so a team of engineers can share an app without routing every deploy through its owner. Tests: `deploy-service.test.ts` (who may redeploy an app), `permissions.test.ts`, `access-actions.test.ts`.
- **SEC-2** (fixed). Both routes now take the right to manage the app, the same as the deploy they follow.
- **SEC-9** (fixed). Following a deploy takes the right to open the app. The route now reconciles through `reconcileDeployment` like every other place a deploy is looked at.
- **SEC-10** (fixed). Removed. It had no callers.
- **DEPLOY-1** (fixed). A deploy now carries an `EnvChange` (set these, unset those), never the whole set. The provider reads the current variables back from Google and applies the change, and the recorded names are merged the same way. Taking a variable away is an explicit `unset`. Tests: `provider.test.ts` (variables on a redeploy), `processes.test.ts`, `deploy-service.test.ts`. Design: `docs/secrets.md`.
- **DEPLOY-2** (fixed). The next variables are written into a revision that takes no traffic, pinned to the one serving. The new code and the new variables go live together at the swap. Workers and jobs beside a web service keep their variables until the swap. An app that is only workers and jobs has nowhere to hold them, so for it they still apply when the deploy starts (documented in `docs/secrets.md`). Tests: `provider.test.ts`, `processes.test.ts` (an app with a web service and a worker).
- **DEPLOY-3** (fixed). New terminal status `superseded` (migration 0032). Starting a deploy supersedes every older one of the app still in flight, and reconciling checks for a newer deploy before asking the provider, since asking is what rolls a build out. A superseded deploy changes nothing about the app and emails nobody. Tests: `deploy-service.test.ts` (two deploys of one app).
- **DEPLOY-4** (fixed). `teardown` removes every job, worker pool and timetable before the service and images. It runs for every service an app ever ran under. Tests: `processes.test.ts` (takes every process down when the app is torn down), `app-teardown.test.ts`.
- **DEPLOY-5** (fixed). A redeploy passes the service name from the app's last deployment, so renaming an app or its space no longer creates a second service. Teardown removes every name an app ever ran under, which also cleans up apps renamed before this fix. Tests: `provider.test.ts`, `app-teardown.test.ts`.
- **DEPLOY-8** (fixed). A first deploy that never got going removes the app it created, so a retry is the same app at the same address. A redeploy that never got going puts the app back to its previous status, because its last good version is still serving. Tests: `deploy-service.test.ts`.
- **SEC-3** (fixed). The CLI sends what the build would be sent anyway: `.dockerignore` under a Dockerfile, the repository's `.gitignore` files otherwise, and `.ciraignore` over both. Files that hold credentials by convention are held back and named, and a `.pem` or `.key` is decided by whether it holds a private key, so a CA bundle still ships. Cira refuses to show such files to the model even if an older CLI sends them (`source-pack.ts`). Tests: `files.test.ts`, `bundle.test.ts`.
- **DEPLOY-6** (fixed). The build is given the browser-public variables and nothing else: `--env` for buildpacks and `--build-arg` for a Dockerfile, with `$` escaped for Cloud Build. One list of public prefixes (`public-env.ts`) now drives this, the CLI's warning and the app page. Tests: `provider.test.ts`.
- **DEPLOY-7** (fixed). `dist`, `build`, `out` and `target` (and `.class` files) are only left out of buildpacks builds. Under a Dockerfile, `.dockerignore` decides. Tests: `files.test.ts`.
- **DEPLOY-9** (fixed). `.env`, `.env.production`, `.env.local` and `.env.production.local` are layered, each over the one before. A value typed at the prompt is saved to the most specific file read, quoted so it reads back unchanged. Tests: `env.test.ts`.
- **DEPLOY-10** (fixed). The server waits 35 minutes, the full build timeout plus rollout, before calling a deploy abandoned. The CLI waits as long, and a deploy another one replaced now says so.
- **DEPLOY-11** (fixed). Why a deploy failed is kept (`deployments.failure_reason`, migration 0033), in plain words and with Google's console links taken out (`explainStartFailure`, `explainBuildFailure`). It is shown by the CLI, on the app's deploy history and in the email to managers. Tests: `provider.test.ts`.
- **DEPLOY-21** (fixed). The dotenv reader handles multiline double-quoted values, `\\n` escapes and trailing comments, and every flag takes `--flag=value` as well as `--flag value`. Tests: `env.test.ts`.
- **DEPLOY-22** (fixed). `cira remove --confirm NAME` removes without asking. A bare `--yes`, or no terminal and no name, now says why nothing was removed instead of quietly doing nothing.
- **DEPLOY-23** (fixed). Polling stops at once on a 401 or 404 and says why. The upload has a size-scaled timeout and one retry.
- **SEC-8** (fixed). An upload URL is only issued to someone who belongs to a space.
- **PEOPLE-1** (fixed). Members can be removed, change role and leave (`member-actions.ts`, rules in core's `membership-rules.ts`). A space may have several owners; only owners make or change owners; the last owner cannot leave, be removed or step down. Everything a person leaves behind goes in one act: their apps get a new owner (the remover, or on leaving the longest-standing owner), their grants and team places go. Controls are on the members page. Tests: `membership-rules.test.ts`, `member-actions.test.ts`.
- **PEOPLE-2** (fixed). Admins see invites that are out and unused on the members page, with a Revoke beside each.
- **PEOPLE-3** (fixed). Admins create, rename and delete teams and tick who is on each (`team-actions.ts`, `teams-panel.tsx`). Deleting a team removes its grants.
- **PEOPLE-4** (fixed). `slugify` trims a dash left by cutting, and `slugWithSuffix` keeps `-2` and the random fallback inside 48 characters. Tests: `id.test.ts`.
- **PEOPLE-5** (fixed). `RESERVED_SPACE_SLUGS` in core, skipped when a space picks its address. A test reads the `app` folder and fails if a top-level page is not reserved (`reserved-routes.test.ts`).
- **PEOPLE-6** (fixed). A space named only in another script keeps its name and gets the address `space` (or `space-2`). An app renamed that way keeps the address it has.
- **PEOPLE-7** (fixed). Spaces are listed in the order they were joined, so the landing space is always the same one.
- **PEOPLE-8** (fixed). Every MCP result names its company, and when results span more than one the capability name carries it too (`acme/crm.list_customers`).
- **PEOPLE-9** (fixed). Accepting an invite twice at once is a success rather than a unique-index error, and two spaces created with the same name at once get a sentence, not a stack.
- **PEOPLE-10** (fixed). Found when the first real invite was accepted on production. The invite page now asks a person Cira has no name for to give one, with joining on the same button; the members page no longer repeats an address used as a name.
- **PEOPLE-11** (fixed). Roadmap 3.1. SAML through Clerk (verified against its development instance with mocksaml.com), SCIM of Cira's own. Tested in scim.test.ts through the real routes and a real database: pushed before and after first sign-in, deactivated in Entra's dialect and reactivated in Okta's, groups as teams with their app grants, and the last owner kept.
- **SEC-4** (fixed). Tokens carry a scope (migration 0035). `cli` tokens deploy; `assistant` tokens only reach MCP. Tokens made in the app, and by `cira mcp connect` (new `/api/cli/assistant-token`), are assistant tokens, so an assistant's config no longer holds deploy rights. Every token lapses after ninety days unused, and each use moves that on. The login page asks someone arriving by link to confirm they ran `cira login` themselves. `CIRA_TOKEN` lets CI deploy without a config file. Tests: `cli-session.test.ts`.
- **SEC-5** (fixed). Joining by domain is a setting admins turn on (`spaces.join_by_domain`, migration 0034), off for every space including existing ones. Someone removed cannot rejoin by domain until invited (`space_join_blocks`). The personal-provider list grew from 26 to around 190 domains plus academic ones. Tests: `member-actions.test.ts`, `email-domain.test.ts`.
- **SEC-6** (fixed). Re-linking by address only happens when the old sign-in no longer exists and the account is from before production sign-in or belongs to no space. Otherwise the person lands on `/account-conflict`, which says who can fix it. A re-link revokes the old holder's tokens. An email change that collides with a stale row no longer locks the person out. Tests: `identity.test.ts`.
- **BILL-1** (fixed). A space's plan is worked out from its dates (`effectivePlan` in core): a trial past its fourteen days, or a space whose subscription ended, is `lapsed`. Deploys are refused with a sentence naming who can fix it, and `enforcePlan` switches off workers, scheduled runs and warm apps. Everything deployed still opens. Admins are emailed three days before and on the day. Tests: `plans.test.ts`, `plan-enforcement.test.ts`, `deploy-service.test.ts`.
- **BILL-2** (fixed). `syncQuantities` adds a line for any price with a quantity and none yet, and removes one that falls to zero. A missing price no longer reads as Stripe being unreachable: the admin is told it is not set up, and the operator gets a log naming the lookup key. Tests: `billing.test.ts`.
- **BILL-3** (fixed). When a subscription ends (or Stripe marks it unpaid), `enforcePlan` runs at once from the webhook and again on every watcher pass, and admins are told what was switched off. Tests: `plan-enforcement.test.ts`.
- **BILL-4** (fixed). Checkout is offered whenever the space is not paying, measured by the subscription's status rather than by ever having been a Stripe customer. A former customer also gets a Past invoices link.
- **BILL-5** (fixed). A webhook event is only a pointer: the subscription is fetched as it is now and that is what is written, so arrival order cannot matter. `checkout.session.completed` and `invoice.payment_failed` are handled as well. Tests: `billing.test.ts`.
- **BILL-6** (fixed). Checkout refuses a space that already pays and sends it to the portal. The customer and checkout calls carry idempotency keys, and a second running subscription for a paying space is cancelled when it arrives. Tests: `billing.test.ts`.
- **BILL-7** (fixed). Deleting a space expires its open checkouts, and a subscription that arrives for a space that no longer exists is cancelled. Tests: `billing.test.ts`.
- **BILL-8** (fixed). The watcher probes an app with a sidecar only while someone would notice: kept warm, opened in the last hour, or called in the last fifteen minutes. Single-container apps are billed per request and are always probed.
- **BILL-9** (fixed). Deletion asks Stripe as it is now. A subscription set to cancel at the end of its period, or one that expired, no longer blocks it.
- **BILL-10** (fixed). Admins are emailed when a payment fails and when a subscription ends. The Stripe customer's address follows whoever last started a checkout. `unpaid`, where Stripe leaves a subscription after its last retry, now ends the paid plan instead of keeping it for ever. Tests: `billing-rules.test.ts`.
- **BILL-11** (fixed). Removing an app records the names Google knows its services and processes by (`removed_apps`, migration 0037), and the month's usage still counts them, marked removed.
- **BILL-12** (fixed). One person may own at most two spaces that have never been subscribed.
- **BILL-13** (fixed). Every `v1` in the header is checked, so rotating the webhook secret works. Tests: `billing.test.ts`.
- **BILL-14** (fixed). Coming back from checkout before Stripe confirms shows that the payment was received and is being confirmed, and the page refreshes until it is.
- **BILL-15** (fixed). Keep-warm is written down first, then asked of Google, and put back if Google refuses.
- **ALERT-1** (fixed). The message and whoever it has not reached are kept on the row (migration 0036), and the watcher retries up to five times over a day. Sends are spaced for Resend's rate limit. Tests: `notifications.test.ts`.
- **ALERT-2** (fixed). Each notice has a topic. There are at most two outage emails per topic in two hours and one failing-run email in six, and no 'back up' for an outage nobody was told about. What is held back is recorded. Tests: `notifications.test.ts`.
- **ALERT-4** (fixed). Spaces - trial notices, plan enforcement, billing sync - now come before app checks, and a pass stops starting checks after four minutes.
- **ALERT-5** (fixed). Found while checking who the new trial notices would reach. Recipients now leave out anyone whose id carries the demo mark (`isInventedPerson` in core, `DEMO_MARK` shared with the seed). Tests: `notifications.test.ts`.
- **CAP-1** (fixed). Risk is decided by the method: anything but GET or HEAD is a write, whatever the model called it; the model can only make a GET more cautious (`riskFor` in packages/core/src/capability.ts). Tested in capability.test.ts.
- **CAP-2** (fixed). An enabled write asked for over MCP now returns `needs_approval` with a link to /approve/<id>, and runs only when called again with that approvalId after the person approved it there. An approval is for one person, one capability and one input (compared by a canonical hash, key order ignored), works once (spent atomically), and lapses after 15 minutes (apps/web/src/lib/approvals.ts, migration 0038). Ask Cira keeps its in-window confirmation. Signing in from an approval or CLI link now comes back to it. Tested in capability-engine.test.ts ("a write over MCP") and identity.test.ts.
- **CAP-5** (fixed). On redeploy, an enabled capability whose risk rose, or a write whose method or path moved, is switched off and goes back to review (`reconcileCapabilities`). Tested in capability.test.ts.
- **CAP-3** (fixed). A read with a path parameter that answers 404 is kept when the 404 is not the one the app gives a path nobody wrote (content type or body differ), or when OPTIONS finds the route; when nothing can tell, it stays unconfirmed and is asked again, never deleted (`missingRecordOrRoute` in lib/capability-verify.ts). Tested in capability-verify.test.ts.
- **CAP-4** (fixed). A write that redirects, times out, or trickles its body is reported as 'may still have been made: check in the app before trying again', never as unreachable; a write the app accepted with HTML or too large a body is reported as done (`accepted: true`). Tested in capability-engine.test.ts ("a call, on the wire").
- **CAP-7** (fixed). A read that redirects is refused when it goes to sign in (a login-like path or another origin) and left unconfirmed otherwise. A write whose OPTIONS gives no Allow, or answers every path alike (CORS middleware, detected with a control OPTIONS), is asked with a safe GET instead: 405 means served. Tested in capability-verify.test.ts.
- **CAP-8** (fixed). One 15-second deadline covers the headers and the body, so a trickling app is cut off and the run recorded. Verification samples at most 4 KB of a 404 under its own deadline. Tested in capability-engine.test.ts.
- **CAP-9** (fixed). A run is written down before the call and counted with itself in; a run the limit refuses is taken back out. A burst of parallel calls now passes at most the room left. Tested in capability-engine.test.ts.
- **CAP-10** (fixed). Calls go to the newest deployment that went live (`servingDeployment` in lib/queries.ts), not the newest deployment, so capabilities keep working while a build runs and after one fails. Tested in capability-engine.test.ts.
- **CAP-11** (fixed). `content-length` is the body's UTF-8 byte length. Tested with accents and CJK in capability-engine.test.ts.
- **CAP-15** (fixed). `isSafeTargetPath` refuses a `.` segment and an empty one (`//` anywhere), and an empty string no longer fills a path parameter. Tested in capability.test.ts.
- **CAP-16** (fixed). A refusal met while verifying as one person (with an identity assertion) is not recorded for the capability. Tested in capability-verify.test.ts.
- **CAP-18** (fixed). An agent is handed at most 100,000 characters: pretty when it fits, compact when that fits, otherwise cut with a note asking for less (`forAnAgent` in lib/mcp.ts). Each run records the approval it used (`invocations.approval_id`, migration 0039), and an app's run history says 'approved'. Tested in mcp.test.ts and capability-engine.test.ts.
- **CAP-6** (fixed). Every Ask request, answers to confirmations included, counts against a daily ceiling of 300 per person beside the 100 questions, so a forged history ending on a made-up tool call cannot buy unlimited model turns. The history a browser sends back is capped at 400,000 characters in total (`MAX_HISTORY_CHARS`) and the body at 1 MB. Tested in conversation.test.ts.
- **CAP-12** (fixed). Ask Cira sends the space it was opened in, and every tool it drives (search, describe, invoke, status) is limited to that space (`inSpace` on the tool context). MCP still reaches every space the person can open. Tested in capability-engine.test.ts and conversation.test.ts.
- **CAP-13** (fixed). The analysis has 64,000 tokens for its answer (Haiku 4.5's ceiling) and the source budget came down to 420 KB to make room. An answer cut off at the limit is asked again for the 40 operations people would use most, instead of yielding nothing. Source is packed by likelihood of declaring routes (a language-agnostic pattern over the first 64 KB of each file), then other code, then prose and styles, then tests, instead of alphabetically. Tested in source-pack.test.ts.
- **CAP-17** (fixed). A real call that gets 404 at a path with no parameters asks the app for a made-up path too; only when the two 404s are the same page (the router's, not the handler's 'no such order') is the capability sent back to `pending`, so verification asks again and deletes it if the route is really gone (`recordAbsence`, `sameNotFound`). Verification also keeps any read whose 404 is in the app's own words. Tested in capability-engine.test.ts, console.e2e.test.ts and capability-verify.test.ts.
- **DEPLOY-12** (fixed). What a deploy takes down is decided per kind: a process that keeps its name and becomes a scheduled run has its worker pool removed, and back again. Tested in processes.test.ts.
- **DEPLOY-13** (fixed). Every process write is tried and every failure kept, so one refusal no longer stops the rest; beside a web service the deploy goes out with a `warning` naming what Google refused (`deployments.warning`, migration 0040), shown by the CLI and in the app's deploy history. A web app that goes live while its workers could not follow onto the build says so too, since nothing polls after live. Tested in processes.test.ts.
- **DEPLOY-14** (fixed). Worker pools and jobs are listed page by page (`nextPageToken`), not one page of 500. Tested in processes.test.ts with a fake that pages.
- **DEPLOY-17** (fixed). A deploy that no longer declares a web process reads its variables from the old service and removes the service once the build is ready and the processes are on it; if Google refuses, the deploy goes live with a warning saying the old address still answers. Tested in processes.test.ts.
- **DEPLOY-15** (fixed). A web process's size is read from fly.toml ([[vm]] for the serving group, or the general one) or app.json (`formation.web`), settled to 512 MB, 1, 2 or 4 GB, and kept as `apps.declared_memory_mib`; a manager can choose one under Settings (`apps.memory_mib`, `setAppMemory`), which stands over the repository's. The size is carried through every rollout, changed at Google without a deploy (`setMemory`), shown in the allowance line, and priced by usage and the keep-warm estimate (migration 0041). Tested in processes.test.ts, limits.test.ts, the CLI's processes.test.ts, provider.test.ts and deploy-service.test.ts.
- **DEPLOY-16** (fixed). The deploy route has 300 seconds, and process writes go four at a time instead of one after another. If a request is still cut off before it writes its record, the app no longer says 'deploying' for good: after ten minutes with no deploy in flight it goes back to live (a live deploy exists) or failed (`settleStrandedApps`, run with the abandoned-deploy sweep). Tested in deploy-service.test.ts.
- **E2E-1** (fixed). Found deploying a monorepo to production: one deploy, no other, reported superseded at once. `isSuperseded` now compares against the row's own timestamp in the database and never the row itself. The regression test reads the row back as production does and failed before the fix. Tested in deploy-service.test.ts.
- **E2E-2** (fixed). `--help` and `-h` print usage for any command, and `deploy` and `remove` refuse an argument they do not understand, saying nothing was changed (`unknownArgument` in packages/cli/src/args.ts). Tested in args.test.ts and on the built binary.
- **E2E-3** (fixed). The app is named after the folder being deployed. Part of the DEPLOY-18 change.
- **DEPLOY-18** (fixed). Run from inside a package of a pnpm, yarn, npm or bun workspace, the CLI uploads the workspace root (minus the workspace's other apps) and deploys that package: with its own Dockerfile when it has one, buildpacks for a non-JavaScript package, and otherwise a Dockerfile Cira writes into the upload that installs from the root lockfile and builds the package after its dependencies (turbo when present) and declares every browser-public variable as a build argument (packages/cli/src/workspace.ts). From the root, several apps are no longer a dead end: the CLI prints the `cd <app> && cira deploy` for each. Tested in workspace.test.ts and with a Turborepo deploy to production.
- **DEPLOY-20** (fixed). A Procfile `release:` line or fly.toml `release_command` now runs, once per deploy, on the new build with the new variables, before anything takes traffic. It is a Cloud Run job of its own (not a process, never swapped or switched on); the provider holds the rollout with `release: needed` until told it is done, and the start is claimed by one poll through a conditional write on the deploy's row (migration 0042), so concurrent polls run a migration once. A failed release fails the deploy with nothing rolled out, and its patience restarts when it starts. The CLI says what will run. Tested in processes.test.ts (Google fake) and deploy-service.test.ts (three concurrent polls, one run).
- **E2E-4** (fixed). Found when the Turborepo deploy's build failed. `GET /api/cli/deploy/logs` returns the last 40 lines of a deploy's build output to anyone who may open the app (the same people the app page shows it to; a build holds no secrets), and the CLI prints them under the failure.
- **DEPLOY-24** (fixed). Switching a process on claims room first, in one transaction that locks the space's row and then counts and writes (`claimRoom`), so concurrent switches cannot pass the plan's limit; only then is Google asked, and the record is put back if Google refuses, so a worker is never running while Cira's records (and so its bill) say it is off. Tested in process-actions.test.ts with three switches at once.
- **SEC-7** (fixed). An old address is followed only for someone who may open the app at its new one (`movedAppFor` in lib/authz.ts, used by the app, logs and console pages); anyone else gets the same 404 as for an address nothing ever answered on. Tested in access-actions.test.ts.
- **SEC-11** (fixed). Closed on Cira's side without touching the proxy: a removed app's address is kept in `removed_apps.slug` (migration 0043), and a new app is not given an address another app left within a proxy session's lifetime (`SESSION_SECONDS`), so no session issued for the old app can open the new one. Renamed apps already kept their old address for good. Tested in deploy-service.test.ts. Also made that file's tests independent of how many deploys ran before each.
- **E2E-5** (fixed). Found on the Turborepo deploy, when Cira's Anthropic account had run out of credit. Analysis failures are now said in plain words (busy, unavailable, too large) with what still works, and the raw error goes to Sentry for whoever runs Cira (`analysisFailure`). Tested in capability-analyzer.test.ts, which also covers the large-app second pass.
- **ENG-1** (fixed). `tsconfig.test.json` typechecks every package's sources and tests together, emitting nothing, as the last step of `pnpm typecheck` (so in CI and the publish workflow). The ten drifted tests were brought up to date, one of them mapping database rows to a Capability instead of casting them.
- **CAP-14** (fixed). GET, HEAD and DELETE carry input in the query, a list as the key once per item. A write's input goes as its JSON body except the fields its schema marks `"x-cira-in": "query"`, which the analyzer is now asked to mark for parameters a handler reads from the query string. Tested in capability-engine.test.ts.
- **CAP-19** (fixed). Found on production by redeploying `notify-e2e` with only a variable changed. A capability is now also the same one when a new name is served at the method and path of one that is gone, and it keeps the old name (`reconcileCapabilities`; two candidates on either side stay new rather than guess). The analyzer is also shown the previous list and asked to keep names for routes still served. Tested in capability.test.ts and capability-analyzer.test.ts.
- **CAP-20** (fixed). The deploy's own check now also asks about what the previous build confirmed, keeping it callable while asking, as the person deploying for an app told who is calling; a refusal from the new build is recorded and its managers are emailed. Opening a page still never re-asks what works. The CLI's "Those 1 are real routes" now reads right for one, and names the setting that lets agents in. Tested in capability-engine.test.ts.
- **CAP-21** (fixed). Found deploying `db-e2e` on production: `POST /api/notes`, beside a working `GET /api/notes`, came back "does not serve". A 404 to OPTIONS no longer settles a write as absent; the safe GET that follows does, since it says 404 only for a path that is really not there. Tested in capability-verify.test.ts.
- **CAP-22** (fixed). Found deploying `cache-e2e` on production: its counter was already at 3 before anyone ran it. The GET that settles an inconclusive write is only ever safe when the write is not itself a GET, and CAP-21 widened when that GET is sent. A write whose own method is GET or HEAD is now left for its first real call to settle. Tested in capability-verify.test.ts.
- **DEPLOY-19** (fixed). A site with no server - a Vite, Create React App, Astro, Vue CLI or Parcel build with no start script, or a folder with an index.html - is built with the repository's own package manager and its public variables, and served by nginx on Cloud Run's port with a fallback to index.html (packages/cli/src/static-site.ts); inside a workspace it is built the workspace's way. Tested in static-site.test.ts and by deploying a Vite app to production, which went live.
- **DEPLOY-25** (fixed). Found by breaking `notify-e2e`'s build on production. The stored log is now read as a person would read their own `docker build` (`readableBuildLog`): each Dockerfile step and what it printed, without Cloud Build's wrapping, BuildKit's step numbers and timers, layer downloads, pip notices or Docker's repeated recap - on the app page and in the terminal alike. The terminal also stops pointing at the page for logs it is printing. Tested in build-log.test.ts with the real lines.
- **DEPLOY-26** (fixed). Found on production after breaking `notify-e2e`'s build: the email said the earlier version was still running, the page said Failed and offered no way in. A failed deploy now leaves an app with an earlier live build marked live; the page and the `/enter` door resolve against the build that is serving, with a line saying the last deploy failed (or that a new one is deploying) and a link to it; `app_status` says the same. When the watcher has found the address not answering, the page says "Not answering since" with a link to the logs, and `app_status` carries it as `web`. Tested in app-state.test.ts and notifications.test.ts.
- **DEPLOY-27** (fixed). The deploy status now says, once live, who can open the app besides its owner and how many of its processes are off, and the CLI words its last lines from that. Found redeploying `notify-e2e` with its nightly run switched on.
- **DEPLOY-28** (fixed). Roadmap 3.4, Postgres half. A deploy can ask for a database (`--database`, or yes when offered for a missing `DATABASE_URL`); Cira makes the app its own Neon project and sets the pooled and direct addresses with the deploy's other variables. A deploy that goes no further takes the new database back out; removing the app deletes it first; a `DATABASE_URL` the app already has is never replaced. Tested in databases.test.ts, deploy-service.test.ts and database.test.ts.
- **DEPLOY-29** (fixed). Found reading the proxy's token route while adding custom domains, after DEPLOY-26 had made the page offer Open in exactly that case. The route now reaches the serving build. The proxy also has tests for the first time (`workers/app-proxy/src/index.test.ts`), including one app's session refused on another app's name.
- **DEPLOY-30** (fixed). Caused while shipping custom domains. Recovered by rotating `CIRA_PROXY_SECRET` in Vercel and the Worker together and redeploying; sessions renewed on their own. The ship script now asks Cira whether it accepts the secret (`/api/proxy/domain` answers 401 to a wrong one) and deploys nothing if not. Verified by opening an app end to end afterwards.
- **DEPLOY-31** (fixed). Roadmap 3.4, caches half: the same arrangement as DEPLOY-28, on Upstash. The CLI's database code became one module for both (`addons.ts`). Tested in caches.test.ts, deploy-service.test.ts and addons.test.ts; not yet run against Upstash itself, which needs Cira's Upstash account.
- **E2E-6** (fixed). Found by deploying a Flask app whose release command fails, in production: the deploy was correctly refused and the old version kept serving, but the output shown was the successful build's. `deployOutput` (lib/deploy-output.ts) shows the release run's own log for a deploy that failed at its release, in the CLI and in the app's deploy history, and the CLI says "running the release command" while it runs. Tested in deploy-output.test.ts.
- **E2E-7** (fixed). Found reading the live pricing page. Pricing and the terms now say what happens: apps keep answering, workers and scheduled runs stop, no new deploys until it is paid again, nothing deleted. Worth the lawyer's eye with the rest of the terms.
- **UX-1** (fixed). Sign-in and sign-up sit in Cira's own entry frame: the mark, what the person is starting (a 14-day trial with no card, and why to use a work email), and links back to Cira, pricing and privacy, with Clerk's duplicate heading hidden (components/auth-frame.tsx). Checked on production. Whether sign-up asks for a password or an emailed code is a Clerk dashboard setting, left to the owner.
- **E2E-8** (fixed). Found signed in on production. Keeping an app warm now says it is billed at the Team plan's always-on price, and the process panel says a worker is billed at the plan's worker price whatever its memory (or that the trial includes one), both from PLANS rather than from the cost estimate.
- **E2E-9** (fixed). Found as React error #418 on the logs page. Times are formatted only once the page has hydrated, with a placeholder before (`useHydrated` and `LocalTime` in components/local-time.tsx), in the logs and the process panel.
- **UX-2** (fixed). Every page names itself under a root template (`%s · Cira`), and every page inside a space by its space or app ("Logs · Backend Only Service"), looked up through the same access checks as the page (lib/page-title.ts). All seen on production, signed in and out.
- **E2E-10** (fixed). Found signing in on production with an emailed code. Clerk's card keeps its heading, where each step says what it wants; the frame keeps only the mark and the sentence the card cannot say. Sign-up now asks for an emailed code, not a password (Clerk dashboard).
- **E2E-11** (fixed). A neutral placeholder, the Billing page titled Billing, and an approval titled by what it does ("Regenerate report?").
- **ALERT-3** (fixed). Measured with a worker on production that exits with code 3 every few seconds: the pool calls itself ready throughout, and Cloud Run writes "Container called exit(N)." once per exit. A worker's log is now read once for both that and out-of-memory kills (`readTrouble`); three exits in an hour is a crash loop, shown as "Keeps stopping" with the exit code on the app page, in app_status for assistants, and alerted by the watcher. Tested in processes.test.ts (with the real lines) and notifications.test.ts. The alert that arrived in Gmail read "the worker worker keeps stopping", since most Procfiles call theirs "worker"; a worker of that name is now "its worker" and any other is quoted (messages.test.ts). The crash-e2e app was removed afterwards. The count now reads "exited 12 times in the last hour, with code 3" (`crashCount`, in core), where the two numbers used to run together.
- **ALERT-6** (fixed). Written before 2.7, both said an app behind its own sign-in could never let Cira in. The email now names "Tell this app who is calling" and links to the app's settings, or, when that is already on, says the app's code is not accepting the statement; the Refused group on the app page says the same to whoever manages the app. Tested in messages.test.ts.
- **ALERT-7** (fixed). Run names are quoted in the failed-run email, as worker names are. Found while taking roadmap 1.7 through each alert on production with `notify-e2e`: a failed deploy, a failed scheduled run and a capability that stopped letting Cira in all reached the inbox. Tested in messages.test.ts.
